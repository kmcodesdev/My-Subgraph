{
  "name": "F",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ F",
    "create-local": "graph create --node http://localhost:8020/ F",
    "remove-local": "graph remove --node http://localhost:8020/ F",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 F"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
